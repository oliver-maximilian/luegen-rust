{
  "name": "luegen-rust",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"cargo run -- --debug\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend && cargo build --release",
    "start": "npm run build && cargo run --release"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
